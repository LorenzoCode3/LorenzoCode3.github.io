---
const { category, date, title, text, link } = Astro.props;
---
<div class="py-8 flex flex-wrap md:flex-nowrap">
    <div
        class="md:w-64 md:mb-0 mb-6 flex-shrink-0 flex flex-col"
    >
        <span class="font-semibold title-font text-gray-400"
            >{category}</span
        >
        <span class="mt-1 text-gray-500 text-sm"
            >{date}</span
        >
    </div>
    <div class="md:flex-grow">
        <h2
            class="text-2xl font-medium text-gray-300 title-font mb-2"
        >
            {title}
        </h2>
        <p class="leading-relaxed text-gray-400">
            {text}
        </p>
        <a class="text-indigo-500 inline-flex items-center mt-4 gap-2" href={link}
            >Learn More 
            <i class="fa-solid fa-arrow-right"></i>
        </a>
    </div>
</div>